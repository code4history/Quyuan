{
  "name": "my-lit",
  "version": "0.0.0",
  "main": "dist/my-element.es.js",
  "exports": {
    ".": "./dist/my-element.es.js"
  },
  "types": "types/my-element.d.ts",
  "files": [
    "dist",
    "types"
  ],
  "scripts": {
    "dev": "vite",
    "prebuild": "rimraf dist/*",
    "build": "tsc && vite build",
    "build:swiper": "npm run prebuild && tsc && vite build --config vite.qy-swiper.config.ts",
    "build:viewer": "npm run prebuild && tsc && vite build --config vite.qy-viewer.config.ts",
    "build:lib": "npm run prebuild && tsc && vite build --config vite.qy-swiper.config.ts && vite build --config vite.qy-viewer.config.ts -- --ignoreEmpty"
  },
  "dependencies": {
    "font-awesome": "^4.7.0",
    "lit": "^2.0.2",
    "swiper": "^8.0.1",
    "three": "^0.105.2",
    "viewerjs": "^1.10.3"
  },
  "devDependencies": {
    "amd-to-es6": "^0.2.0",
    "rimraf": "^3.0.2",
    "typescript": "^4.4.4",
    "vite": "^2.7.2"
  }
}
